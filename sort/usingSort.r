v1 <- c(3, 7, 2, 6)
v2 <- c(3, 7, 2, NA, 6)

#sort()
#Return the sorted data immediately (rearrange according to the order and output the value)

sort(v1)

sort(v1, decreasing = T)
#Sort Descending
 
sort(v2)
#Ignore NA (default)
